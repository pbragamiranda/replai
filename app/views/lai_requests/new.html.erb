<div class="container">
  <h1>Faça o seu pedido!</h1>
  <%= simple_form_for @lai_request,
          wrapper: :horizontal_form,
          wrapper_mappings: {
          boolean:       :horizontal_boolean,
          check_boxes:   :horizontal_collection,
          date:          :horizontal_multi_select,
          datetime:      :horizontal_multi_select,
          file:          :horizontal_file,
          radio_buttons: :horizontal_collection,
          range:         :horizontal_range,
          time:          :horizontal_multi_select
    } do |f| %>
    <%= f.error_notification %>
    <%= f.input  :title %>
    <%= f.association :city_government_agency, label_method: :city_name, value_method: :id %>
    <%= f.input  :description%>
    <%= f.input  :category, collection: LaiRequest::CATEGORIES %>
    <%= f.input  :format%>
    <%= f.submit "Finalizar formulário", class: "btn btn-primary" %>
  <% end %>
</div>
